name: EODCdask
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - mamba
  - jupyter
  - numpy
  - pandas
  - matplotlib
  - dask
  - dask-gateway
  - dask-expr
  - graphviz
  - python-graphviz
  - xarray
  - pystac-client
  - odc-stac
  - odc-geo
  - zarr

  #conda env update --file EODCenv.yml --prune
  