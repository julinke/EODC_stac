name: EODC
channels:
  - conda-forge
  - defaults
dependencies:
  - numpy
  - pandas
  - matplotlib
  - dask
  - dask-gateway
  - dask-expr
  - graphviz
  - python-graphviz
  - xarray
  - pystac-client
  - odc-stac
  - odc-geo
  - zarr

  #conda env update --file EODCenv.yml --prune
  